import styled from "styled-components";

export const LoaderElement = styled.div`
  width: 3rem;
  height: 3rem;
  border-radius: 50%;
  border: solid 8px var(--gray);
  border-top: solid 8px var(--black);
  margin: auto;
  animation: spin 1s infinite linear;
`
